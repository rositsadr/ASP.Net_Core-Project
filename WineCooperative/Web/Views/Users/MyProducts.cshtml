@model List<UserProductsViewModel>

@{
    ViewData["Title"] = "My Products";
}

<div class="container">
    @if (Model.Count == 0)
    {
        <div class="text-center">
            <h2>You have no wine products.</h2>
            <a asp-controller="Products" asp-action="Add">Add a product!</a>
        </div>
    }
    <div class="row">
        @foreach (var wine in Model)
        {
        <div class="card mb-3" style="max-width: 540px;">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src=@wine.ImageUrl class="img-fluid rounded-start" alt="Image">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">@wine.Name</h5>
                        <p class="card-text mb-0">Year: @wine.ManufactureYear</p>
                        <p class="card-text mb-0">Price: @wine.Price lv.</p>
                        <p class="card-text mb-0">Color: @wine.Color</p>
                        <p class="card-text mb-0">Taste: @wine.Taste</p>
                        <p class="card-text mb-0">Manufacturer: @wine.Manufacturer</p>
                        <p class="card-text mb-0">Wine area: @wine.WineArea</p>
                        <p class="card-text mb-0">In Stock: @(wine.InStock?"Yes":"No")</p>
                        <p class="card-text mb-0">@wine.Description</p>
                    </div>
                </div>
                <div class="text-center" >
                    <a asp-controller="Products" asp-action="Edit" asp-route-id=@wine.Id>Edit wine</a>
                </div>
            </div>
        </div>
        }
    </div>
</div>
